# Zerene-minecraft-plugin

Create a plugin that can connect to a MongoDB database. 
  - For now, just make a command that will allow an admin (permission node) to add points to a user using there IGN. 
